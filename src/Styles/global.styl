*
  padding 0
  margin 0
  box-sizing border-box
.Header
  padding 5px
  display flex
  justify-content space-between
  align-content center
  box-shadow inset 0 0 8px var(--black-light)
  &-Logo
    height 40px
